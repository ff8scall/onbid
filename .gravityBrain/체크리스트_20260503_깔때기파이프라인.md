# ✅ 작업 체크리스트: 깔때기형 AI 분석 파이프라인 (Funnel Architecture)

## [현재 수행 단계: MASC 분석 완료 / 남은 할 일: 깔때기 파이프라인 구현 / 작업 중인 파일: `ai_analyzer.py`]

### 1. 텍스트 전처리 및 1차 필터링 (Stage 0)
- [x] `ai_analyzer.py`: HTML/JSON 태그 제거 정규식 함수(`clean_text`) 추가
- [x] `ai_analyzer.py`: Rule-based 필터링 강화
    - [x] 상품권 가격 이상(액면가 < 최저가) 매물 필터링
    - [x] 필수 키워드 부재 매물 즉시 탈락 처리

### 2. Maverick 기반 광역 필터링 (Stage 1)
- [x] `ai_analyzer.py`: 배치 처리 로직 구현 (`get_maverick_batch_analysis`)
    - [x] 50개 매물 단위로 리스트 구성
    - [x] 80점 이상 매물의 ID와 선별 이유만 JSON으로 추출하는 프롬프트 작성
    - [x] 배치 응답 결과 DB 반영 (`is_maverick_selected = 1`)

### 3. Gemini Flash 기반 정밀 분석 (Stage 2)
- [x] `ai_analyzer.py`: 정예 매물 대상 Deep Dive 로직 구현 (`get_flash_deep_dive`)
    - [x] Gemini 1.5 Flash API 연동
    - [x] **외부 시세 교차 검증**: 시세 추정 로직 고도화
    - [x] **리스크 판독**: 공고문 내 독소 조항(인수 책임 등) 정밀 분석
    - [x] **마케팅 카피**: 사용자 친화적 3줄 요약 생성

### 4. DB 스키마 확장
- [x] `init_db.py`: 단계별 분석 상태 추적을 위한 컬럼 추가
    - [x] `is_maverick_selected` (Maverick 통과 여부)
    - [x] `ai_deep_dive_report` (최종 상세 리포트 내용)

### 5. 대시보드 UI 연동
- [x] `page.tsx`: '최종 정예 매물(Flash 완료)'과 '후보 매물(Maverick 통과)' 구분 노출
- [x] `ItemDetail`: Flash가 생성한 상세 리포트 및 리스크 경고 표시 UI 강화
