# 📋 체크리스트: 데이터 중복 처리 및 AI 분석 고도화 (2026-05-04)

## 1. 개요
- **목표**: 
  - 동일 매물 중복 노출 방지 (Upsert 로직 도입)
  - 가격 변동 시에만 AI 재분석 수행 (비용 및 효율 최적화)
  - AI 분석 보고서에 '수익 발생 근거(Price Gap 사유)' 구체화
- **대상 파일**:
  - `backend/collector.py`: 수집 및 DB 저장 로직
  - `backend/ai_analyzer.py`: AI 분석 프롬프트 및 파이프라인

## 2. 작업 상세 (TODO)

### 2.1. `backend/collector.py` 수정
- [x] `search_it_items()` 함수 내 `INSERT OR REPLACE`를 `INSERT ... ON CONFLICT`로 변경.
- [x] 가격(`min_bid_prc`) 변동 여부를 감지하여 `is_ai_processed` 플래그 관리 로직 추가.
  - 가격이 변했다면 `is_ai_processed = 0` (재분석 대상).
  - 가격이 그대로라면 기존 분석 결과 유지.
- [x] `collect_details()` 함수(있는 경우)에도 동일 로직 적용.

### 2.2. `backend/ai_analyzer.py` 수정
- [x] `FLASH_DEEP_DIVE_PROMPT` 수정:
  - "왜 이 매물이 시세보다 저렴하게 나올 수 있는지(차액 발생 사유)"에 대한 상세 설명을 요구하는 지침 추가.
  - 출력 JSON 스키마에 `reason_for_price_gap` 필드 추가.
- [x] `run_pipeline()` 내 결과 저장 로직에 신규 필드 반영.

### 2.3. 검증 (Validation)
- [ ] 로컬 DB에서 동일 `pbanc_mng_no`, `cltr_mng_no`에 대해 가격 변경 시 플래그가 초기화되는지 확인.
- [ ] AI 분석 결과에 차액 발생 사유가 포함되는지 확인.
- [ ] Vercel/서버 배포 및 정상 작동 확인.

## 3. 리스크 및 주의사항
- **DB 스키마**: `onbid_items` 테이블에 `reason_for_price_gap` 필드가 없으므로 스키마 확장이 필요할 수 있음. (또는 `ai_curator_comment`나 `ai_deep_dive_report` 내부에 포함)
- **데이터 유실**: Upsert 시 기존 `ai_score` 등 분석 데이터가 보존되도록 `UPDATE` 구문을 정교하게 작성해야 함.
