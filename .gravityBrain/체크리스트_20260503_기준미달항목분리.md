# ✅ 작업 체크리스트: 기준 미달 매물 분리 및 Stepfun AI 연동

## [현재 수행 단계: MASC 분석 및 설계 완료 / 남은 할 일: DB 마이그레이션 및 AI 연동 / 작업 중인 파일: `init_db.py`, `ai_analyzer.py`]

### 1. 데이터베이스 및 백엔드 기반 작업
- [x] `init_db.py`: `is_substandard` 컬럼 추가 (INTEGER DEFAULT 0)
- [x] `ai_analyzer.py`: Stepfun AI 연동 (NVIDIA NIM 적용)
    - [x] `NVIDIA_API_KEY` 및 `NVIDIA_BASE_URL` 환경 변수 설정 대응
    - [x] `stepfun-ai/step-3.5-flash` 모델 및 파라미터(Temp 1.0, Top_p 0.9) 적용
    - [x] **Rate Limit 구현**: 1분에 10회 미만 (평균 6.5초당 1회) 제한 로직 추가
- [x] `ai_analyzer.py`: 로직 수정
    - [x] 기존 `DELETE` 로직을 `is_substandard = 1` 업데이트 로직으로 변경
    - [x] `is_target_item = 0` 이거나 `score < 60`인 경우 기준 미달로 분류

### 2. API 서빙 고도화
- [x] `main.py`: 기준 미달 항목 조회를 위한 엔드포인트 추가 (`/items/substandard`)
- [x] `main.py`: 기존 `/items` 엔드포인트에서 `is_substandard = 0` 필터링 기본 적용

### 3. 프론트엔드 UI 연동
- [x] `Dashboard`: '기준 미달 항목 보기' 탭 또는 토글 추가
- [x] `ItemList`: 기준 미달 항목에 대해 '탈락 사유' 표시 UI 구현

### 4. 검증 및 테스트
- [ ] Stepfun API 호출 및 Rate Limit 정상 작동 확인 (API Key 설정 필요)
- [ ] DB에 기준 미달 매물이 정상적으로 저장되고 조회되는지 확인
