# 📋 체크리스트: 메뉴별 다중 정렬 기능 고도화 (2026-05-07)

## 1. MASC 분석 및 설계
- [x] **PM**: 각 메뉴(정예, 후보, 미달, 종료)별 최적의 정렬 UI 배치 및 기본 정렬값 정의
- [x] **Architect**: 탭 전환 시 정렬 상태 유지 및 `expired` 메뉴의 역순 정렬 로직 설계
- [x] **Security**: 정렬 데이터 무결성 확인 및 예외 처리 (수익금 0원 등)

## 2. 프론트엔드 구현 (`frontend/src/app/page.tsx`)
- [x] **정렬 상태 관리 고도화**:
  - [x] `sortMode` 외에 `sortOrder` (asc/desc) 지원 여부 검토 (기본은 사용자 요청에 따른 최적순)
  - [x] 메뉴 전환 시 해당 메뉴에 특화된 기본 정렬 적용 (예: 정예는 수익금순)
- [x] **정렬 로직 정밀화**:
  - [x] `expired` 메뉴에서는 마감일이 최근인 순서대로 정렬 (내림차순)
  - [x] `target` 메뉴에서는 수익금 높은 순서 (내림차순)
  - [x] `candidate` 메뉴에서는 점수 높은 순서 (내림차순)
- [x] **UI/UX 폴리싱**:
  - [x] 정렬 버튼에 아이콘 추가 및 활성 상태 시각적 강조 (Glassmorphism 적용)
  - [x] "수익금순", "마감임박순", "스코어순" 텍스트를 더 직관적으로 변경

## 3. 검증 및 완료
- [x] 각 메뉴별로 정렬 버튼이 정상 작동하는지 확인
- [x] 데이터가 없는 경우(null/undefined) 정렬 순위가 밀려나는지 확인
- [x] `/finish-task` 프로토콜 수행 및 메모리 업데이트
