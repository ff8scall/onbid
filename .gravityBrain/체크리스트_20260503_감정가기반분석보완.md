# ✅ 체크리스트: 감정평가액 기반 분석 보완 및 판별 로직 정교화 (완료)

## 1. 분석 및 설계 (MASC)
- [x] **PM**: 상세 텍스트가 부족하더라도 감정평가액(Appraisal Value)이 있는 매물(금, 귀금속 등)은 분석 대상에 포함하여 '진짜 노다지' 매물 발굴.
- [x] **Architect**: 
    - `classify_item_type`에서 `apsl_evl_amt > 0`인 경우 `MISSING` 판별 기준 완화 (귀금속/명품 한정).
    - `FLASH_DEEP_DIVE_PROMPT`에 감정평가액 변수 추가 및 분석 가이드라인 보강.
    - `collector.py`에서 감정평가액 데이터를 정수형으로 안정적으로 수집하도록 개선.
- [x] **Security**: 변화 없음.

## 2. 코드 수정 (`backend/ai_analyzer.py`)
- [x] `classify_item_type` 함수 수정:
    - 귀금속/명품 카테고리이면서 감정가가 존재하면 `SINGLE`로 간주하여 분석 진행.
- [x] `get_flash_deep_dive` 함수 수정:
    - 프롬프트에 `appraisal_price` 데이터 주입.
    - AI에게 "감정평가액 대비 입찰가율"을 분석 지표로 활용하도록 지시.

## 3. 데이터 수집 수정 (`backend/collector.py`)
- [x] `apsl_evl_amt` 필드 수집 및 정수 변환 로직 강화.
- [x] `pbancMngNo` 필드 매핑 최종 확인 (onbidPbancNo 활용).

## 4. 검증 및 마무리
- [x] 귀금속 매물(금반지, 골드바 등)이 '보류'되지 않고 정상적으로 분석되어 '정예 매물'로 올라오는 것 확인 (87건 분석 성공).
- [x] `MEMORY.md`, `SYSTEM_MAP.md`, `CORE_LOGIC.md` 현행화.
