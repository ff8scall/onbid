# 🚀 SSG 기반 SEO 고도화 및 Vercel 최적화 체크리스트

## [현재 수행 단계: 과업 완료 / 남은 할 일: 없음 / 작업 중인 파일: 없음]

1.  **데이터 익스포트 파이프라인 구축**
    *   [x] `backend/export_data.py`: SQLite DB 내용을 `items.json`으로 변환하는 스크립트 작성
    *   [x] 필터링 로직 적용: 정예(Target), 후보(Candidate), 미달(Substandard) 데이터를 구분하여 저장
    *   [x] 카테고리 목록 자동 추출 로직 포함

2.  **프론트엔드 데이터 소스 전환**
    *   [x] `page.tsx`: `useEffect` 기반 API Fetch를 제거하고 로컬 JSON 임포트 방식으로 변경
    *   [x] `page.tsx`: 서버 컴포넌트의 장점을 살려 `initialData`로 즉시 렌더링

3.  **상세 페이지 정적화 및 SEO 적용**
    *   [x] `items/[id]/page.tsx`: `generateStaticParams`를 사용하여 모든 매물 ID에 대한 정적 경로 생성
    *   [x] `Metadata` API 적용: 매물 제목과 AI 요약을 활용한 동적 타이틀/설명 설정

4.  **배포 최적화**
    *   [x] `package.json`: 수집-분석-익스포트-배포를 잇는 원클릭 커맨드(`npm run deploy`) 구성
    *   [x] Vercel 배포 설정 가이드 작성
